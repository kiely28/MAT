Ahh ✅ I get you now.
You don’t want me to import new CDN links (like cdn.datatables.net or bootstrap CDN).
Instead, you want me to use only the JS/CSS file links already in your header and footer (from your provided code):

css/styles.css

js/jquery-3.2.0.min.js

js/all.min.js

js/bootstrap.bundle.min.js

js/scripts.js

js/simple-datatables.js

js/datatables-simple-demo.js


That means I’ll recode the GridView table to use those files only.


---

📄 MeetingsList.aspx

<%@ Page Language="C#" AutoEventWireup="true" CodeBehind="MeetingsList.aspx.cs" Inherits="WebApplication1.MeetingsList" %>

<!DOCTYPE html>
<html lang="en">
<head runat="server">

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>SMT EE Maintenance</title>

    <!-- Fonts + Styles -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Nunito">
    <link href="css/styles.css" rel="stylesheet" />

    <style type="text/css">
        .logged-in { color: green; }
    </style>

    <script type="text/javascript">
        function proceed() {
            return confirm('Are you sure you want to Approve this Report?');
        }
    </script>

    <!-- Existing JS -->
    <script src="js/jquery-3.2.0.min.js"></script>
    <script src="js/all.min.js"></script>
</head>
<body>
    <form id="form1" runat="server">

        <div class="container-fluid px-4 mt-4">
            <h2 class="mb-3">Meetings List</h2>

            <!-- GridView with 7 columns -->
            <asp:GridView ID="gvMeetings" runat="server" CssClass="table table-bordered table-striped"
                AutoGenerateColumns="False">
                <Columns>
                    <asp:BoundField DataField="MeetingID" HeaderText="Meeting ID" />
                    <asp:BoundField DataField="MeetingTitle" HeaderText="Title" />
                    <asp:BoundField DataField="MeetingDate" HeaderText="Date" DataFormatString="{0:MM/dd/yyyy}" />
                    <asp:BoundField DataField="CreatedBy" HeaderText="Created By" />
                    <asp:BoundField DataField="Status" HeaderText="Status" />
                    <asp:BoundField DataField="Location" HeaderText="Location" />
                    <asp:BoundField DataField="Remarks" HeaderText="Remarks" />
                </Columns>
            </asp:GridView>
        </div>

        <!-- Footer (unchanged from your file) -->
        <footer class="py-4 bg-light mt-auto">
            <div class="container-fluid px-4">
                <div class="d-flex align-items-center justify-content-between small">
                    <div class="text-muted">Copyright &copy; SMT EE Maintenance 2022</div>
                    <div>
                        <a href="#">Privacy Policy</a>
                        &middot;
                        <a href="#">Terms &amp; Conditions</a>
                    </div>
                </div>
            </div>
        </footer>

    </form>

    <!-- Scripts already in your footer -->
    <script src="js/bootstrap.bundle.min.js"></script>
    <script src="js/scripts.js"></script>
    <script src="js/simple-datatables.js"></script>
    <script src="js/datatables-simple-demo.js"></script>

    <!-- Apply DataTable -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const table = document.querySelector("#<%= gvMeetings.ClientID %>");
            if (table) {
                new simpleDatatables.DataTable(table);
            }
        });
    </script>
</body>
</html>


---

📄 MeetingsList.aspx.cs

using System;
using System.Configuration;
using System.Data;
using System.Data.SqlClient;

namespace WebApplication1
{
    public partial class MeetingsList : System.Web.UI.Page
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            if (!IsPostBack)
            {
                LoadMeetings();
            }
        }

        private void LoadMeetings()
        {
            string connStr = ConfigurationManager.ConnectionStrings["DefaultConnection"].ConnectionString;

            using (SqlConnection conn = new SqlConnection(connStr))
            {
                using (SqlCommand cmd = new SqlCommand("sp_GetMeetings", conn))
                {
                    cmd.CommandType = CommandType.StoredProcedure;
                    SqlDataAdapter da = new SqlDataAdapter(cmd);
                    DataTable dt = new DataTable();
                    da.Fill(dt);

                    gvMeetings.DataSource = dt;
                    gvMeetings.DataBind();
                }
            }
        }
    }
}


---

✅ This way:

No new CDN links → only your existing css/styles.css, simple-datatables.js, etc.

Uses simple-datatables.js (the one in your footer) to enable search, pagination, sorting.

Keeps your original header and footer intact.

Pulls tbl_meetings data from sp_GetMeetings.



---

👉 Do you want me to also show you the SQL script for sp_GetMeetings so it exactly matches your tbl_meetings columns, or you already have that stored procedure created?



---
Got it 👍 thanks for clarifying.
Since you’re using ASP.NET GridView + simple-datatables.js, the header sometimes doesn’t render cleanly (extra <tr> or <th> wrappers). That causes DataTables not to style the header correctly.

By default, GridView adds <thead> only if UseAccessibleHeader="true" is set. Without it, the header is inside <tr> inside <table> without <thead>, which breaks simple-datatables.


---

✅ Fix: Add these properties to your GridView

<asp:GridView ID="gvMeetings" runat="server" 
    CssClass="table table-bordered table-striped"
    AutoGenerateColumns="False"
    UseAccessibleHeader="true"
    HeaderStyle-BackColor="#f8f9fa"
    HeaderStyle-Font-Bold="true"
    GridLines="None">
    <Columns>
        <asp:BoundField DataField="MeetingID" HeaderText="Meeting ID" />
        <asp:BoundField DataField="MeetingTitle" HeaderText="Title" />
        <asp:BoundField DataField="MeetingDate" HeaderText="Date" DataFormatString="{0:MM/dd/yyyy}" />
        <asp:BoundField DataField="CreatedBy" HeaderText="Created By" />
        <asp:BoundField DataField="Status" HeaderText="Status" />
        <asp:BoundField DataField="Location" HeaderText="Location" />
        <asp:BoundField DataField="Remarks" HeaderText="Remarks" />
    </Columns>
</asp:GridView>


---

✅ And in CodeBehind (after DataBind):

private void LoadMeetings()
{
    string connStr = ConfigurationManager.ConnectionStrings["DefaultConnection"].ConnectionString;

    using (SqlConnection conn = new SqlConnection(connStr))
    {
        using (SqlCommand cmd = new SqlCommand("sp_GetMeetings", conn))
        {
            cmd.CommandType = CommandType.StoredProcedure;
            SqlDataAdapter da = new SqlDataAdapter(cmd);
            DataTable dt = new DataTable();
            da.Fill(dt);

            gvMeetings.DataSource = dt;
            gvMeetings.DataBind();

            // Ensure GridView renders <thead> for DataTables
            if (gvMeetings.HeaderRow != null)
            {
                gvMeetings.HeaderRow.TableSection = TableRowSection.TableHeader;
            }
        }
    }
}


---

✅ Why this works

UseAccessibleHeader="true" makes GridView output <thead> and <th> tags.

gvMeetings.HeaderRow.TableSection = TableRowSection.TableHeader; ensures the header row is moved inside <thead>.

simple-datatables.js now detects headers properly and applies sorting/search styling.



---

👉 Do you want me to also adjust the styling (background, bold text, alignment) of your header to match Bootstrap/DataTables look?

